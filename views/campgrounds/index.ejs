<% include ./../partials/header %>


<div class="container">
	<header class="jumbotron">
		<div class="container">
			<h1>Welcome to BookShare</h1>
			<p>Share Books, Share Knowledge</p>
			<p>
				<a class="btn btn-primary btn-lg" href="/campgrounds/new">Add Your Book</a>
			</p>
		</div>
	</header>
	
	<div class="row" style="display:flex; flex-wrap: wrap;">
		<% campgrounds.forEach(function(campground) {	%>
			<div class="col-md-3 col-sm-6">
				<div class="card text-center">
					<img src="<%= campground.image %>" class="card-img-top">
					<div class="card-body">
						<div class="card-title"><%= campground.name %></div>
						<a href="/campgrounds/<%= campground._id%>" class="btn btn-primary btn-sm">More Info</a>
					</div>
				</div>
			</div>
		<% }) %>
	</div>
</div>
<% include ./../partials/footer %>
